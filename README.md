# githired
Job search via GitHub Jobs API

# README

GitHub Jobs search example application

# System dependencies

* `rbenv install 2.5.0`
* `brew install postgres`

# Configuration

* `bundle install`
* `bin/rspec`
